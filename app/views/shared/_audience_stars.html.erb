<% count = local_assigns[:count].to_i %>
<% if count > 0 %>
  <span class="text-sm font-bold text-ink text-shadow-brand"
        role="img"
        aria-label="<%= count %> audience <%= count == 1 ? 'star' : 'stars' %>">
    <%= count %> ⭐
  </span>
<% end %>
