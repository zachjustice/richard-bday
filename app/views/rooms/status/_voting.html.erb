<div class="status-page-spacing voting-container">
  <div class="voting-card">
    <div class="prompt-section">
      <div class="prompt-icon">🗳️</div>
      <h2 class="prompt-text"><%= @game_prompt.prompt.description %></h2>
      <p class="prompt-instruction">Vote for the best answer on your device!</p>
    </div>

    <div class="progress-section">
      <h3>Voting Progress</h3>
      <div class="progress-grid">
        <div class="progress-stats">
          <div class="stat-card stat-completed">
            <span class="stat-number"><%= @votes.count %></span>
            <span class="stat-label">Completed</span>
          </div>
          <div class="stat-card stat-waiting">
            <span class="stat-number"><%= @users.count - @votes.count %></span>
            <span class="stat-label">Waiting</span>
          </div>
        </div>

        <div class="completed-users">
          <ul id="users-with-submitted-answers" class="completed-list">
            <% @users.each do |user| %>
              <li 
                id="submitted-answer-<%= user %>" 
                class=<%= @users_with_vote.include?(user.name) ? "completed-user" : "answering-user" %>
              >
                <%= user.name %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
