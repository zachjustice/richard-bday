<span id="waiting-room-audience-count" class="badge-primary transition-opacity duration-200 <%= 'hidden' if audience_count == 0 %>" aria-live="polite" aria-atomic="true">
  <%= audience_count %> watching
</span>
