# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

# TODO: re-enable recurring jobs once we size up off the 1 GB droplet.
# An empty production section means Solid Queue does not spawn a scheduler
# process, saving ~150 MB of RAM on the host. Until then, run these manually
# every so often via `bin/kamal app exec`:
#   SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)
#   CleanupExpiredTokensJob.perform_now
production:
  # clear_solid_queue_finished_jobs:
  #   command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
  #   schedule: every hour at minute 12
  # cleanup_expired_tokens:
  #   class: CleanupExpiredTokensJob
  #   queue: default
  #   schedule: every day at 3am

development:
  cleanup_expired_tokens:
    class: CleanupExpiredTokensJob
    queue: default
    schedule: every day at 3am
